import React from 'react'

const baseLayout = ({ ...myProps }) => {
    return (
        <div style={{ position: 'fixed', height: '100%', width: '100%', top: 0 }}>
            hello
        </div>
    )
}
export default baseLayout